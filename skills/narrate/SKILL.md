# Narrate

Use the `narrate` MCP prompt when the user wants a short status update.
Use the `stream` MCP prompt when the user wants ongoing progress updates.

Keep narration brief, phase-based, and free of secrets or raw diffs.
